import { Routes } from '@angular/router';
import { PasswordCardComponent } from './components/password-card/password-card.component';

export const routes: Routes = [
    {path:'', component:PasswordCardComponent}
];
